# git-via-proxy

stdio MCP server for Web Cursor / Cursor Desktop: run `git` and allowlisted network binaries with a local HTTP proxy.

Wired by the Agent Gateway when `GIT_VIA_PROXY_URL` is set and `GIT_VIA_PROXY_MCP=1`.

```bash
npm install --omit=dev
```
